So the user wants a single HTML file they can open locally in their browser—no server needed. They'll use a visual builder interface to create templates, and the tool will export PHP code in the same format as templates.php that they can manually copy into their actual file. Since it's entirely client-side, I'm building this with pure HTML, CSS, and JavaScript using either canvas or DOM elements.

The key constraint is that the exported PHP code needs to work with the existing render.php system without requiring them to modify it. So I'll need to add a generic layout renderer function to render.php once (a dispatch case for custom layouts), then the builder outputs PHP that feeds into that handler. This way the output is immediately compatible when they paste it in. The rendering engine needs to live in the toolkit's PHP backend, so I'm adding a generic layout renderer to engine.php that handles recursive node trees with full styling support—backgrounds, borders, shadows, flexbox layout, text properties, icons, and images. Then I'll set up a $CUSTOM_SPECS array in templates.php where users paste the generated template definitions, register it as a category that only appears when populated, and add a dispatch case in render.php to route custom templates through the new layout renderer. The standalone generator itself will be a downloadable HTML file that users run locally in their browser to design and export templates.

For the builder interface, I need to create a self-contained HTML file with inline CSS and JavaScript that provides buttons to add elements (boxes, text, icons, images), a property inspector for editing selected elements with controls for backgrounds, padding, borders, shadows, dimensions, and flexbox properties, and icon codepoint mappings that match what the renderer uses so exported templates stay compatible. I'm adding the canvas size presets dropdown, a sample data panel for realistic previews, and an export button that generates the final PHP code block defining the template structure with all the node tree data. I'll also include a template ID field with slug validation and inline documentation explaining how to integrate the exported code into the project by pasting it into the templates.php file. Now I need to inspect render.php to identify reusable helper functions so the builder and rendering engine stay consistent.